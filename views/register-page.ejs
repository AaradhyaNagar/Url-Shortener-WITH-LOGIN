<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Secure Url Shortener Registration Page</title>
    <link rel="stylesheet" href="register.css" />
  </head>
  <body>
    <h1>Secured URL Shortener</h1>
    <div class="container">
      <h3>Sign Up</h3>
      <form method="POST" action="/register">
        <div class="nameContainer">
          <input
            class="fName"
            type="text"
            required
            name="firstName"
            placeholder="First Name"
          />
          <input
            class="LName"
            type="text"
            required
            name="lastName"
            placeholder="Last Name"
          />
        </div>
        <input type="text" required name="email" placeholder="Email address" />
        <input 
          type="password"
          required
          name="password"
          placeholder="Password"
        />
        <input
          type="password"
          required
          name="reEnterPassword"
          placeholder="Re-Enter Password"
        />
        <button class="signUp" type="submit">Sign Up</button>
      </form> 
      <div class="separator"></div>
      <form method="GET" action="/app">
        <button class="backToLogIn" type="submit">Back To Log In</button>
      </form>
      <div class="separator"></div>
      <form method="GET" action="/">
        <button class="backToHome" type="submit">Back To Home</button>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="errorMessage"><%= error %></div>
          <% } %>
      </form>
    </div>
  </body>
</html>
